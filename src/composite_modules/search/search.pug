//- Объявление миксина
mixin mixSearch(params)
    .search
        .search__title=params.title
        .search__dropdown-date-split
            +mixDropdownDateSplit(params.dropdownSplitParams)
        .search__guests
            +mixDropdown(params.dropdownParams)
        .search__button
            +mixButtonArrow(params.buttonArrowParams)